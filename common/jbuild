(jbuild_version 1)

(library
 ((name common)
  (public_name pyre.common)
  (preprocess
    (pps
      (ppx_deriving.eq
       ppx_deriving.show
       ppx_hash
       ppx_compare
       ppx_sexp_conv)))
  (libraries
    (core sexplib ppxlib hack_parallel))))
